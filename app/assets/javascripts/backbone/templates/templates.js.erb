!function(){
  <% Dir.glob("#{Rails.root}/app/assets/javascripts/backbone/templates/**/*.html.haml").each do |template|     %>
ich.addTemplate(
"<%= File.basename template, '.html.haml' %>",
"<%= Tilt.new(template).render.gsub(/\"/, '\\\"').gsub(/\n/,'') %>"
);
  <% end %>
 }();
// 2012-09-19 17:50:26 -0400
